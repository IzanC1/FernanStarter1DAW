ADMINISTRADOR > PANEL DE CONTROL
- Añadir opción de volver.

ADMINISTRADOR > MODIFICAR  
- Añadir opción 4 para volver.

GESTOR > GENERAL
- Asegurarse de que donde se solicitan números, efectivamente se haya introducido un número. Esto se puede hacer con `scanner.hasNextInt` y un `do-while` (opcional).
- Donde se solicite texto, usar `trim()`.

GESTOR > MODIFICAR PROYECTO  
- Al modificar un proyecto, preguntar al usuario, por cada campo, si desea modificarlo o no. Si no lo desea, saltar al siguiente campo.  
- Para una versión más detallada, implementar un sistema de selección con un menú donde cada campo tenga asignado un número y el usuario pueda elegir qué modificar. Incluir también una opción para salir en cualquier momento.

GENERAL
- Terminar de implementar las barras de progreso.  
- **(IMPORTANTE)** Utilizar `trim()` y `toLowerCase()` en todo lo relacionado con cambios de nombre de usuario/contraseña. **(IMPORTANTE)**
